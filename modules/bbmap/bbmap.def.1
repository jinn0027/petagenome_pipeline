Bootstrap: localimage
From: ../common/el9.sif

%setup

%files

%post
    alias dnf="LD_LIBRARY_PATH=/usr/local/lib dnf"
    pushd /opt
        # install bbmap
        echo "############ bbmap ###############"
        /opt/miniconda3/bin/mamba install -y -q bbmap=39.13
    popd

%environment

%runscript
