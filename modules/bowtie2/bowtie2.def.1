Bootstrap: localimage
From: ../common/el9.sif

%setup

%files

%post
    alias dnf="LD_LIBRARY_PATH=/usr/local/lib dnf"
    pushd /opt
        # install bowtie2
        echo "############ bowtie2 ###############"
        /opt/miniconda3/bin/mamba install -y -q \
            bowtie2=2.5.4
    popd

%environment

%runscript
