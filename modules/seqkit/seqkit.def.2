Bootstrap: localimage
From: ../common/el9.sif

%setup

%files

%post
    alias dnf="LD_LIBRARY_PATH=/usr/local/lib dnf"
    pushd /opt
        # install seqkit
        echo "############ seqkit ###############"
        /opt/miniconda3/bin/mamba install -y -q \
            seqkit=2.9.0
    popd

%environment

%runscript
