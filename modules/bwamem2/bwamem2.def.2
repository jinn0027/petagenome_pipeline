Bootstrap: localimage
From: ../common/el9.sif

%setup

%files

%post
    alias dnf="LD_LIBRARY_PATH=/usr/local/lib dnf"
    pushd /opt
        # install bwa-mem2
        /opt/miniconda3/bin/mamba install -y -q \
            bwa-mem2=2.2.1
    popd

%environment

%runscript

